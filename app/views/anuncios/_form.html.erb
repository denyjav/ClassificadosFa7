

<%= form_for @anuncio do |f| %>
  <% if @anuncio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anuncio.errors.count, "error") %> prohibited this anuncio from being saved:</h2>

      <ul>
      <% @anuncio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="panel panel-warning">
  <div class="panel-heading">Novo anúncio</div>
  <div class="panel-body">
  <div class="field ">
    <%= f.label :imagem %><br>
    <%= f.file_field :imagem %>
  </div>
   <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descricao%><br>
    <%= f.text_area :descricao %><br>
  </div>
  <div class="field">
    <%= f.label :preco %><br>
    <%= f.text_field :preco %>
  </div>
   <div class="field">
    <%= f.label :favorito %><br>
    <%= f.check_box :favorito %>
  </div>
  <div class="field">
    <%= f.label :categoria %><br>
    <%= select("anuncio", "categoria", Anuncio.select('distinct categoria').collect {|p| [ p.categoria, p.categoria ] }, {prompt: 'Selecione a categoria'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  </div>
  </div>
  </div>
<% end %>
